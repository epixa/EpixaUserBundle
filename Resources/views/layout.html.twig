<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
</head>
<body>
    {% for key, message in app.session.getFlashes() %}
    <div class="flash {{ key }}">
        {{ message }}
    </div>
    {% endfor %}

    <div>
        {% block epixa_user_content %}
        {% endblock epixa_user_content %}
    </div>
</body>
</html>